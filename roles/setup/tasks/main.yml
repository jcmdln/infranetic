# SPDX-License-Identifier: ISC
---
- name: Install packages
  ansible.builtin.package:
    name: "{{ setup_packages }}"
    state: present

- name: Configure dnf-automatic
  include_tasks: dnf-automatic.yml
  when:
    - dnf_automatic_enabled is defined
    - dnf_automatic_enabled|bool
